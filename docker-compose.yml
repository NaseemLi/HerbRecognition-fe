version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: herb-frontend
    restart: always
    ports:
      - "80:80"
    environment:
      - TZ=Asia/Shanghai
    networks:
      - herb-network

networks:
  herb-network:
    driver: bridge
